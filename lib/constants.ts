// Session currency. One place to edit, no hunting.
export const CURRENCY_NAME = "Knots";

// When true, the motivation module opens the session as step zero.
// When false, it renders as a play card on the Harbor dashboard and the
// session starts at the first action module.
export const MOMENT_AS_STEP_ZERO = true;

// Points per completed rep. Commitments are the real proof, so they pay more,
// and the keystone (the one the streak rides on) pays the most.
export const REP_POINTS = 10;
export const COMMITMENT_POINTS = 20;
export const KEYSTONE_POINTS = 30;
